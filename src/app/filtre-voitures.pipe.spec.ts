import { FiltreVoituresPipe } from './filtre-voitures.pipe';

describe('FiltreVoituresPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltreVoituresPipe();
    expect(pipe).toBeTruthy();
  });
});
